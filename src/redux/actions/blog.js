import axios from 'axois'
import { GET_BLOG_LIST_SUCCESS} from './type'

export const get_blog_list=()=> async dispatch=>{
    const config={
        headers:{
            'Accept': 'application/json'
        }
    };
    try{
        const res =await axios.get
    }
}